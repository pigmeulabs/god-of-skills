# Wireframe Reviewer Agent

Use this agent for UI wireframes. It checks layout hierarchy, component alignment, state coverage, spacing, mobile/web proportions, CTA placement, and whether the diagram stays low-fidelity instead of over-styled.
