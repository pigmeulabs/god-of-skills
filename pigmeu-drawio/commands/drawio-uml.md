# /drawio-uml

Create UML diagrams in draw.io.

```bash
/drawio-uml "model users, roles, and permissions" --kind class
/drawio-uml "show login and password reset interactions" --kind sequence
/drawio-uml "model the account lifecycle" --kind state-machine
```
