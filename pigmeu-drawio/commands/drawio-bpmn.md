# /drawio-bpmn

Create BPMN process diagrams with pools, lanes, tasks, events, gateways, and labeled branch flows.

```bash
/drawio-bpmn "customer onboarding with document validation and rejection path"
```
